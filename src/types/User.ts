export type UserData = {
  userId: string;
  username: string;
  email: string;
  pictureUrl: string;
  firstName: string;
  lastName: string;
  headline?: string;
  location?: string;
  bio?: string;
  isFollowing?: boolean;
};
